<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.exemple.com.daddstories_0">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="قصص ض للأطفال"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DADDStories_0">
        <activity
            android:name=".DeutschActivity"
            android:label="Geschichten auf Deutsch"
            android:parentActivityName=".MainActivity"></activity>
        <activity
            android:name=".ArabicActivity"
            android:label="قصص بالعربية"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".EnglishActivity"
            android:label="English Stories"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>